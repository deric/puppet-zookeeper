HOSTS:
  debian-8-x64:
    platform: debian-8-amd64
    hypervisor : docker
    image: debian:8
    roles:
      - agent
    debug: false
    docker_image_commands:
      - 'apt-get install -y wget'
      - 'locale-gen en_US.UTF-8'
CONFIG:
  masterless: true
  log_level: info
  type: foss
